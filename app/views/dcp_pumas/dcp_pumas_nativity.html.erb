<h5>Community District Data</h5>

  <%= render 'shared/nav_boro' %>


  <div>
    <div class="col-9 offset-3 table">
      <div>
        <table class="table table-hover">
          <thead class="tab-content" id="v-pills-tabContent">
            <tr>
              <th scope="col" class="table-h1">
                Community Districts
              </th>
              <th scope="col" class="table-h2">
                Native
              </th>
              <th scope="col" class="table-h2">
                Foreign born
              </th>
            </tr>
          </thead>
        <thead>
        </div>
        <tbody>
          <tr>
            <% @pumas.each do |puma| %>

            <td> <%= puma.name %> </td>
            <td class="tbl">
            <%= puma.pums.where(nativity: '1').length
            %></td>
            <td class="tbl"><%= puma.pums.where(nativity: '2').length
            %></td>
          </tr>
            <td>Total: <%= puma.pums.count(:nativity) %> </td>
          </tr>
           <% end %>
        </tbody>
       
      </div>
    </div>